"use strict";
/**
 * Copyright (c) Microsoft Corporation. All rights reserved.
 * Licensed under the MIT License.
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.ConnectorClient = exports.getProductInfo = void 0;
const agents_telemetry_1 = require("@microsoft/agents-telemetry");
const agents_activity_1 = require("@microsoft/agents-activity");
const errorHelper_1 = require("../errorHelper");
const activityWireCompat_1 = require("../activityWireCompat");
const getProductInfo_1 = require("../getProductInfo");
Object.defineProperty(exports, "getProductInfo", { enumerable: true, get: function () { return getProductInfo_1.getProductInfo; } });
const headerPropagation_1 = require("../headerPropagation");
const agents_telemetry_2 = require("@microsoft/agents-telemetry");
const observability_1 = require("../observability");
const env_1 = require("../utils/env");
const httpClient_1 = require("../httpClient");
const logger = (0, agents_telemetry_1.debug)('agents:connector-client');
function formatHttpErrorMessage(error) {
    var _a;
    const responseData = (_a = error.response) === null || _a === void 0 ? void 0 : _a.data;
    if (responseData === undefined) {
        return error.message;
    }
    try {
        const serializedResponseData = JSON.stringify(responseData);
        return serializedResponseData === undefined ? error.message : `${error.message}: ${serializedResponseData}`;
    }
    catch {
        return error.message;
    }
}
/**
 * ConnectorClient is a client for interacting with the Microsoft Connector API.
 */
class ConnectorClient {
    /**
     * Private constructor for the ConnectorClient.
     * @param httpClient - The HttpClient instance to use for HTTP requests.
     */
    constructor(httpClient) {
        this._httpClient = httpClient;
    }
    get httpClient() {
        return this._httpClient;
    }
    /**
     * Creates a new instance of ConnectorClient with authentication.
     * @param baseURL - The base URL for the API.
     * @param authConfig - The authentication configuration.
     * @param authProvider - The authentication provider.
     * @param scope - The scope for the authentication token.
     * @param headers - Optional headers to propagate in the request.
     * @returns A new instance of ConnectorClient.
     */
    static async createClientWithAuth(baseURL, authConfig, authProvider, scope, headers) {
        const token = await authProvider.getAccessToken(authConfig, scope);
        return this.createClientWithToken(baseURL, token, headers);
    }
    /**
     * Creates a new instance of ConnectorClient with token.
     * @param baseURL - The base URL for the API.
     * @param token - The authentication token.
     * @param headers - Optional headers to propagate in the request.
     * @returns A new instance of ConnectorClient.
     */
    static createClientWithToken(baseURL, token, headers) {
        const headerPropagation = headers !== null && headers !== void 0 ? headers : new headerPropagation_1.HeaderPropagation({});
        (0, getProductInfo_1.applyUserAgentHeader)(headerPropagation);
        headerPropagation.override({
            Accept: 'application/json',
            'Content-Type': 'application/json',
        });
        const clientHeaders = { ...headerPropagation.outgoing };
        if (token && token.length > 1) {
            clientHeaders.Authorization = `Bearer ${token}`;
        }
        const httpClient = new httpClient_1.HttpClient({
            baseURL,
            headers: clientHeaders,
        });
        return new ConnectorClient(httpClient);
    }
    /**
     * Retrieves a list of conversations.
     * @param continuationToken - The continuation token for pagination.
     * @returns A list of conversations.
     */
    async getConversations(continuationToken) {
        return (0, agents_telemetry_2.trace)(observability_1.ConnectorClientTraceDefinitions.getConversations, async ({ record }) => {
            var _a;
            const config = {
                method: 'get',
                url: '/v3/conversations',
                params: continuationToken ? { continuationToken } : undefined
            };
            const response = await this.executeRequest(config);
            record({ httpStatusCode: (_a = response.status) === null || _a === void 0 ? void 0 : _a.toString() });
            return response.data;
        });
    }
    async getConversationMember(userId, conversationId) {
        return (0, agents_telemetry_2.trace)(observability_1.ConnectorClientTraceDefinitions.getConversationMember, async ({ record }) => {
            var _a;
            if (!userId || !conversationId) {
                throw agents_activity_1.ExceptionHelper.generateException(Error, errorHelper_1.Errors.UserIdAndConversationIdRequired);
            }
            const config = {
                method: 'get',
                url: `v3/conversations/${conversationId}/members/${userId}`,
                headers: {
                    'Content-Type': 'application/json'
                }
            };
            const response = await this.executeRequest(config);
            record({ httpStatusCode: (_a = response.status) === null || _a === void 0 ? void 0 : _a.toString() });
            return response.data;
        });
    }
    /**
     * Creates a new conversation.
     * @param body - The conversation parameters.
     * @returns The conversation resource response.
     */
    async createConversation(body) {
        return (0, agents_telemetry_2.trace)(observability_1.ConnectorClientTraceDefinitions.createConversation, async ({ record }) => {
            var _a;
            const payload = {
                ...body,
                activity: (0, activityWireCompat_1.normalizeOutgoingActivity)(body.activity)
            };
            const config = {
                method: 'post',
                url: '/v3/conversations',
                headers: {
                    'Content-Type': 'application/json'
                },
                data: payload
            };
            const response = await this.executeRequest(config);
            record({ httpStatusCode: (_a = response.status) === null || _a === void 0 ? void 0 : _a.toString() });
            return response.data;
        });
    }
    /**
     * Replies to an activity in a conversation.
     * @param conversationId - The ID of the conversation.
     * @param activityId - The ID of the activity.
     * @param body - The activity object.
     * @returns The resource response.
     */
    async replyToActivity(conversationId, activityId, body) {
        return (0, agents_telemetry_2.trace)(observability_1.ConnectorClientTraceDefinitions.replyToActivity, async ({ record }) => {
            var _a;
            logger.debug(`Replying to activity: ${activityId} in conversation: ${conversationId}`);
            record({ conversationId, activityId });
            if (!conversationId || !activityId) {
                throw agents_activity_1.ExceptionHelper.generateException(Error, errorHelper_1.Errors.ConversationIdAndActivityIdRequired);
            }
            const trimmedConversationId = this.conditionallyTruncateConversationId(conversationId, body);
            const config = {
                method: 'post',
                url: `v3/conversations/${trimmedConversationId}/activities/${encodeURIComponent(activityId)}`,
                headers: {
                    'Content-Type': 'application/json'
                },
                data: (0, activityWireCompat_1.normalizeOutgoingActivity)(body),
                ...(body.channelId === agents_activity_1.Channels.Msteams && body.isTargetedActivity() ? { params: { isTargetedActivity: 'true' } } : {})
            };
            const response = await this.executeRequest(config);
            record({ httpStatusCode: (_a = response.status) === null || _a === void 0 ? void 0 : _a.toString() });
            logger.info('Reply to conversation/activity: ', response.data.id, activityId);
            return response.data;
        });
    }
    /**
     * Trim and sanitize the conversationId when creating the URL for agentic calls in agents channels.
     * @param conversationId The ID of the conversation to potentially truncate.
     * @param activity The activity object used to determine if truncation is necessary.
     * @returns The original conversationId for non-agents channels/non-agentic roles, otherwise a trimmed and sanitized value.
     */
    conditionallyTruncateConversationId(conversationId, activity) {
        var _a, _b;
        if (activity.channelIdChannel === agents_activity_1.Channels.Agents &&
            (((_a = activity.from) === null || _a === void 0 ? void 0 : _a.role) === agents_activity_1.RoleTypes.AgenticIdentity || ((_b = activity.from) === null || _b === void 0 ? void 0 : _b.role) === agents_activity_1.RoleTypes.AgenticUser)) {
            const maxLength = (0, env_1.parseIntEnv)(process.env.MAX_APX_CONVERSATION_ID_LENGTH, 150);
            const trimmedConversationId = conversationId.length > maxLength ? conversationId.substring(0, maxLength) : conversationId;
            return trimmedConversationId.replace(/[/\\#?]/g, '_');
        }
        else {
            return conversationId;
        }
    }
    /**
     * Sends an activity to a conversation.
     * @param conversationId - The ID of the conversation.
     * @param body - The activity object.
     * @returns The resource response.
     */
    async sendToConversation(conversationId, body) {
        return (0, agents_telemetry_2.trace)(observability_1.ConnectorClientTraceDefinitions.sendToConversation, async ({ record }) => {
            var _a;
            logger.debug(`Send to conversation: ${conversationId} activity: ${body.id}`);
            if (!conversationId) {
                throw agents_activity_1.ExceptionHelper.generateException(Error, errorHelper_1.Errors.ConversationIdRequired);
            }
            record({ conversationId });
            const trimmedConversationId = this.conditionallyTruncateConversationId(conversationId, body);
            const config = {
                method: 'post',
                url: `v3/conversations/${trimmedConversationId}/activities`,
                headers: {
                    'Content-Type': 'application/json'
                },
                data: (0, activityWireCompat_1.normalizeOutgoingActivity)(body),
                ...(body.channelId === agents_activity_1.Channels.Msteams && body.isTargetedActivity() ? { params: { isTargetedActivity: 'true' } } : {})
            };
            const response = await this.executeRequest(config);
            record({ httpStatusCode: (_a = response.status) === null || _a === void 0 ? void 0 : _a.toString() });
            return response.data;
        });
    }
    /**
     * Updates an activity in a conversation.
     * @param conversationId - The ID of the conversation.
     * @param activityId - The ID of the activity.
     * @param body - The activity object.
     * @returns The resource response.
     */
    async updateActivity(conversationId, activityId, body) {
        return (0, agents_telemetry_2.trace)(observability_1.ConnectorClientTraceDefinitions.updateActivity, async ({ record }) => {
            var _a;
            if (!conversationId || !activityId) {
                throw agents_activity_1.ExceptionHelper.generateException(Error, errorHelper_1.Errors.ConversationIdAndActivityIdRequired);
            }
            record({ conversationId, activityId });
            const config = {
                method: 'put',
                url: `v3/conversations/${conversationId}/activities/${activityId}`,
                headers: {
                    'Content-Type': 'application/json'
                },
                data: (0, activityWireCompat_1.normalizeOutgoingActivity)(body),
                ...(body.channelId === agents_activity_1.Channels.Msteams && body.isTargetedActivity() ? { params: { isTargetedActivity: 'true' } } : {})
            };
            const response = await this.executeRequest(config);
            record({ httpStatusCode: (_a = response.status) === null || _a === void 0 ? void 0 : _a.toString() });
            return response.data;
        });
    }
    /**
     * Deletes an activity from a conversation.
     * @param conversationId - The ID of the conversation.
     * @param activityId - The ID of the activity.
     * @param isTargetedActivity - When true, appends ?isTargetedActivity=true to the request URL.
     * @returns A promise that resolves when the activity is deleted.
     */
    async deleteActivity(conversationId, activityId, isTargetedActivity) {
        return (0, agents_telemetry_2.trace)(observability_1.ConnectorClientTraceDefinitions.deleteActivity, async ({ record }) => {
            var _a;
            record({ conversationId, activityId });
            if (!conversationId || !activityId) {
                throw agents_activity_1.ExceptionHelper.generateException(Error, errorHelper_1.Errors.ConversationIdAndActivityIdRequired);
            }
            const config = {
                method: 'delete',
                url: `v3/conversations/${conversationId}/activities/${activityId}`,
                headers: {
                    'Content-Type': 'application/json'
                },
                ...(isTargetedActivity ? { params: { isTargetedActivity: 'true' } } : {})
            };
            const response = await this.executeRequest(config);
            record({ httpStatusCode: (_a = response.status) === null || _a === void 0 ? void 0 : _a.toString() });
            return response.data;
        });
    }
    /**
       * Uploads an attachment to a conversation.
       * @param conversationId - The ID of the conversation.
       * @param body - The attachment data.
       * @returns The resource response.
       */
    async uploadAttachment(conversationId, body) {
        return (0, agents_telemetry_2.trace)(observability_1.ConnectorClientTraceDefinitions.uploadAttachment, async ({ record }) => {
            var _a;
            record({ conversationId });
            if (conversationId === undefined) {
                throw agents_activity_1.ExceptionHelper.generateException(Error, errorHelper_1.Errors.ConversationIdRequired);
            }
            const config = {
                method: 'post',
                url: `v3/conversations/${conversationId}/attachments`,
                headers: {
                    'Content-Type': 'application/json'
                },
                data: body
            };
            const response = await this.executeRequest(config);
            record({ httpStatusCode: (_a = response.status) === null || _a === void 0 ? void 0 : _a.toString() });
            return response.data;
        });
    }
    /**
     * Retrieves attachment information by attachment ID.
     * @param attachmentId - The ID of the attachment.
     * @returns The attachment information.
     */
    async getAttachmentInfo(attachmentId) {
        return (0, agents_telemetry_2.trace)(observability_1.ConnectorClientTraceDefinitions.getAttachmentInfo, async ({ record }) => {
            var _a;
            record({ attachmentId });
            if (attachmentId === undefined) {
                throw agents_activity_1.ExceptionHelper.generateException(Error, errorHelper_1.Errors.AttachmentIdRequired);
            }
            const config = {
                method: 'get',
                url: `v3/attachments/${attachmentId}`,
                headers: {
                    'Content-Type': 'application/json'
                }
            };
            const response = await this.executeRequest(config);
            record({ httpStatusCode: (_a = response.status) === null || _a === void 0 ? void 0 : _a.toString() });
            return response.data;
        });
    }
    /**
     * Retrieves an attachment by attachment ID and view ID.
     * @param attachmentId - The ID of the attachment.
     * @param viewId - The ID of the view.
     * @returns The attachment as a readable stream.
     */
    async getAttachment(attachmentId, viewId) {
        return (0, agents_telemetry_2.trace)(observability_1.ConnectorClientTraceDefinitions.getAttachment, async ({ record }) => {
            var _a;
            record({ attachmentId, viewId });
            if (attachmentId === undefined) {
                throw agents_activity_1.ExceptionHelper.generateException(Error, errorHelper_1.Errors.AttachmentIdRequired);
            }
            if (viewId === undefined) {
                throw agents_activity_1.ExceptionHelper.generateException(Error, errorHelper_1.Errors.ViewIdRequired);
            }
            const config = {
                method: 'get',
                url: `v3/attachments/${attachmentId}/views/${viewId}`,
                headers: {
                    'Content-Type': 'application/json'
                },
                responseType: 'stream'
            };
            const response = await this.executeRequest(config);
            record({ httpStatusCode: (_a = response.status) === null || _a === void 0 ? void 0 : _a.toString() });
            return response.data;
        });
    }
    async executeRequest(config) {
        var _a, _b, _c;
        const { method, url, data, headers, params } = config;
        const { Authorization, authorization, ...headersToLog } = { ...this._httpClient.defaultHeaders, ...headers };
        logger.debug('Request: ', {
            host: this._httpClient.baseURL,
            url,
            data,
            method,
            params,
            headers: headersToLog
        });
        try {
            const response = await this._httpClient.request(config);
            logger.debug('Response: ', {
                status: response.status,
                statusText: response.statusText,
                host: this._httpClient.baseURL,
                url: (_a = response.config) === null || _a === void 0 ? void 0 : _a.url,
                data: (_b = response.config) === null || _b === void 0 ? void 0 : _b.data,
                method: (_c = response.config) === null || _c === void 0 ? void 0 : _c.method,
            });
            return response;
        }
        catch (error) {
            if (error instanceof httpClient_1.HttpError) {
                const message = formatHttpErrorMessage(error);
                logger.debug('Response error: ', {
                    host: this._httpClient.baseURL,
                    url: error.config.url,
                    method: error.config.method,
                    data: error.config.data,
                    message,
                    stack: error.stack,
                });
                Object.assign(error, {
                    host: this._httpClient.baseURL,
                    url: error.config.url,
                    method: error.config.method,
                    data: error.config.data,
                    message,
                });
                throw error;
            }
            throw error;
        }
    }
}
exports.ConnectorClient = ConnectorClient;
//# sourceMappingURL=connectorClient.js.map