"use strict";
// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License.
Object.defineProperty(exports, "__esModule", { value: true });
exports.ConversationBuilder = void 0;
const conversation_1 = require("./conversation");
const conversationReferenceBuilder_1 = require("./conversationReferenceBuilder");
/**
 * Fluent builder for the `Conversation` class.
 *
 * @example
 * ```typescript
 * // Build from scratch
 * const conv = ConversationBuilder
 *   .create('my-client-id', 'msteams')
 *   .withUser('user-aad-id')
 *   .withConversationId('19:channel@thread.skype')
 *   .build()
 *
 * // Build from a live TurnContext
 * const conv = ConversationBuilder.fromContext(turnContext).build()
 * ```
 */
class ConversationBuilder {
    constructor(agentClientId, channelId, serviceUrl, claims, reference) {
        this._agentClientId = agentClientId;
        this._channelId = channelId;
        this._serviceUrl = serviceUrl;
        this._claims = claims;
        this._reference = reference;
    }
    /**
     * Creates a new builder for the given agent and channel.
     * @param requestorId Optional: the client ID of the app making the request.
     *   Becomes the `appid` claim, used in multi-tenant Azure Bot scenarios.
     */
    static create(agentClientId, channelId, serviceUrl, requestorId) {
        const claims = { aud: agentClientId };
        if (requestorId)
            claims.appid = requestorId;
        return new ConversationBuilder(agentClientId, channelId, serviceUrl !== null && serviceUrl !== void 0 ? serviceUrl : '', claims, conversationReferenceBuilder_1.ConversationReferenceBuilder.create(agentClientId, channelId, serviceUrl).build());
    }
    /**
     * Creates a builder pre-populated from a live `TurnContext`.
     * Captures both the conversation reference and the JWT identity claims.
     */
    static fromContext(context) {
        var _a, _b;
        const ref = context.activity.getConversationReference();
        const id = context.identity;
        const aud = Array.isArray(id === null || id === void 0 ? void 0 : id.aud) ? id.aud[0] : ((_a = id === null || id === void 0 ? void 0 : id.aud) !== null && _a !== void 0 ? _a : '');
        const claims = {
            ...(id !== null && id !== void 0 ? id : {}),
            aud,
        };
        return new ConversationBuilder(aud, ref.channelId, (_b = ref.serviceUrl) !== null && _b !== void 0 ? _b : '', claims, ref);
    }
    /** Sets `reference.user`. */
    withUser(userId, userName) {
        const user = { id: userId, name: userName };
        this._reference = { ...this._reference, user };
        return this;
    }
    /** Sets `reference.conversation.id`. */
    withConversationId(id) {
        var _a;
        const conversation = { ...((_a = this._reference.conversation) !== null && _a !== void 0 ? _a : { isGroup: false }), id };
        this._reference = { ...this._reference, conversation };
        return this;
    }
    /** Sets `reference.conversation` from a full `ConversationAccount`. */
    withConversation(account) {
        this._reference = { ...this._reference, conversation: account };
        return this;
    }
    /** Sets `reference.activityId`. */
    withActivityId(activityId) {
        this._reference = { ...this._reference, activityId };
        return this;
    }
    /**
     * Merges a partial `ConversationReference` into the current one.
     * Useful for overlaying externally-provided reference data without losing
     * fields set by earlier builder calls.
     */
    withReference(ref) {
        this._reference = { ...this._reference, ...ref };
        return this;
    }
    /** Builds the `Conversation`, auto-filling `serviceUrl` from channel defaults if needed. */
    build() {
        var _a, _b;
        const serviceUrl = this._serviceUrl ||
            this._reference.serviceUrl ||
            conversationReferenceBuilder_1.ConversationReferenceBuilder.serviceUrlForChannel(this._channelId);
        const reference = {
            conversation: (_a = this._reference.conversation) !== null && _a !== void 0 ? _a : { id: '', isGroup: false },
            agent: (_b = this._reference.agent) !== null && _b !== void 0 ? _b : { id: this._agentClientId },
            ...this._reference,
            // Ensure channelId and serviceUrl always use our resolved values
            channelId: this._channelId,
            serviceUrl,
        };
        const conv = new conversation_1.Conversation(this._claims, reference);
        conv.validate();
        return conv;
    }
}
exports.ConversationBuilder = ConversationBuilder;
//# sourceMappingURL=conversationBuilder.js.map