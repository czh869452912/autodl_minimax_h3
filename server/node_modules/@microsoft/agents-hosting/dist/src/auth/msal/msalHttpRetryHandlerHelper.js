"use strict";
/**
 * Copyright (c) Microsoft Corporation. All rights reserved.
 * Licensed under the MIT License.
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.MsalHttpRetryHandlerHelper = exports.DEFAULT_MSAL_RETRY_COUNT = void 0;
exports.normalizeMsalRetryCount = normalizeMsalRetryCount;
const agents_activity_1 = require("@microsoft/agents-activity");
const agents_telemetry_1 = require("@microsoft/agents-telemetry");
const errorHelper_1 = require("../../errorHelper");
const logger = (0, agents_telemetry_1.debug)('agents:msal');
const requestTimeoutStatus = 408;
/**
 * The default number of retries made for an MSAL HTTP request.
 */
exports.DEFAULT_MSAL_RETRY_COUNT = 2;
/**
 * Converts a configured MSAL retry count to its effective runtime value.
 */
function normalizeMsalRetryCount(retryCount) {
    if (!Number.isFinite(retryCount)) {
        return exports.DEFAULT_MSAL_RETRY_COUNT;
    }
    return Math.max(0, Math.floor(retryCount));
}
/**
 * MSAL network client that retries responses with HTTP status 408 (Request Timeout).
 *
 * @remarks
 * `maxRetryCount` is the number of retries after the initial request. Responses with
 * any other status are returned immediately and retries have no additional delay.
 */
class MsalHttpRetryHandlerHelper {
    constructor(networkClient = new MsalHttpClient(), maxRetryCount = exports.DEFAULT_MSAL_RETRY_COUNT) {
        this.networkClient = networkClient;
        this.maxRetryCount = normalizeMsalRetryCount(maxRetryCount);
    }
    /**
     * Sends an HTTP GET request, retrying only Request Timeout responses.
     */
    async sendGetRequestAsync(url, options, timeout) {
        return this.sendWithRetries(() => this.networkClient.sendGetRequestAsync(url, options, timeout));
    }
    /**
     * Sends an HTTP POST request, retrying only Request Timeout responses.
     */
    async sendPostRequestAsync(url, options) {
        return this.sendWithRetries(() => this.networkClient.sendPostRequestAsync(url, options));
    }
    async sendWithRetries(send) {
        let response = await send();
        let attempt;
        for (attempt = 0; response.status === requestTimeoutStatus && attempt < this.maxRetryCount; attempt++) {
            logger.debug('MSAL retry on request timeout (retry %d/%d)', attempt + 1, this.maxRetryCount);
            response = await send();
        }
        logger.debug('MSAL request completed with status %d after %d attempt(s)', response.status, attempt + 1);
        return response;
    }
}
exports.MsalHttpRetryHandlerHelper = MsalHttpRetryHandlerHelper;
/**
 * Default MSAL-compatible network implementation. MSAL Node does not export its
 * built-in HTTP client, so the retry handler uses the same native fetch contract.
 */
class MsalHttpClient {
    async sendGetRequestAsync(url, options, timeout) {
        return this.sendRequest(url, 'GET', options, timeout);
    }
    async sendPostRequestAsync(url, options) {
        return this.sendRequest(url, 'POST', options);
    }
    async sendRequest(url, method, options, timeout) {
        var _a;
        const controller = new AbortController();
        const timeoutId = timeout
            ? setTimeout(() => controller.abort(), timeout)
            : undefined;
        let response;
        try {
            response = await fetch(url, {
                method,
                headers: options === null || options === void 0 ? void 0 : options.headers,
                body: method === 'POST' ? options === null || options === void 0 ? void 0 : options.body : undefined,
                signal: controller.signal
            });
        }
        catch (error) {
            const innerException = error instanceof Error ? error : undefined;
            if ((innerException === null || innerException === void 0 ? void 0 : innerException.name) === 'AbortError') {
                throw agents_activity_1.ExceptionHelper.generateException(Error, errorHelper_1.Errors.TokenRequestTimeout, innerException, { timeoutMs: String(timeout) });
            }
            throw agents_activity_1.ExceptionHelper.generateException(Error, errorHelper_1.Errors.MsalHttpRequestFailed, innerException, {
                message: (_a = innerException === null || innerException === void 0 ? void 0 : innerException.message) !== null && _a !== void 0 ? _a : 'unknown'
            });
        }
        finally {
            if (timeoutId) {
                clearTimeout(timeoutId);
            }
        }
        const headers = {};
        response.headers.forEach((value, key) => {
            headers[key] = value;
        });
        return {
            headers,
            body: await this.parseBody(response),
            status: response.status
        };
    }
    async parseBody(response) {
        const text = await response.text();
        try {
            return JSON.parse(text);
        }
        catch (error) {
            if (response.status === requestTimeoutStatus) {
                return {};
            }
            throw agents_activity_1.ExceptionHelper.generateException(Error, errorHelper_1.Errors.MsalResponseUnparsable, error instanceof Error ? error : undefined);
        }
    }
}
//# sourceMappingURL=msalHttpRetryHandlerHelper.js.map