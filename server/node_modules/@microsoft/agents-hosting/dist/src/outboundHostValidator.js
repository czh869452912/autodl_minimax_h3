"use strict";
/**
 * Copyright (c) Microsoft Corporation. All rights reserved.
 * Licensed under the MIT License.
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.OutboundHostValidator = void 0;
exports.loadOutboundHostValidatorOptionsFromEnv = loadOutboundHostValidatorOptionsFromEnv;
exports.createOutboundHostValidator = createOutboundHostValidator;
const env_1 = require("./utils/env");
/** Hosts used by Microsoft channel callbacks and hosted attachments. */
const DEFAULT_MICROSOFT_HOSTS = Object.freeze([
    'botframework.com',
    'smba.trafficmanager.net',
    'teams.microsoft.com',
    'teams.microsoft.us',
    'graph.microsoft.com',
    'sharepoint.com',
    'svc.ms',
    'blob.core.windows.net'
]);
const OUTBOUND_HOST_VALIDATOR_ENV_PREFIX = 'OutboundHostValidator__';
const OUTBOUND_HOST_VALIDATOR_ENV_PREFIX_UPPER = OUTBOUND_HOST_VALIDATOR_ENV_PREFIX.toUpperCase();
/**
 * Shared allowlist policy for server-side outbound requests.
 *
 * A configured suffix matches both the exact host and its subdomains. The
 * policy is disabled by default to preserve existing SDK behavior.
 */
class OutboundHostValidator {
    constructor(options = {}) {
        var _a, _b, _c;
        this.enabled = (_a = options.enabled) !== null && _a !== void 0 ? _a : false;
        const suffixes = new Set();
        if ((_b = options.includeDefaultMicrosoftHosts) !== null && _b !== void 0 ? _b : true) {
            for (const host of DEFAULT_MICROSOFT_HOSTS)
                suffixes.add(host);
        }
        for (const host of (_c = options.hosts) !== null && _c !== void 0 ? _c : []) {
            const normalized = normalizeConfiguredHost(host);
            if (normalized)
                suffixes.add(normalized);
        }
        this.suffixes = suffixes;
    }
    isAllowed(input) {
        if (!this.enabled)
            return true;
        const host = getUrlHost(input);
        if (!host)
            return false;
        for (const suffix of this.suffixes) {
            if (host === suffix || host.endsWith(`.${suffix}`))
                return true;
        }
        return false;
    }
}
exports.OutboundHostValidator = OutboundHostValidator;
/**
 * Loads validator options from environment variables compatible with the
 * .NET `OutboundHostValidator` configuration section.
 *
 * Hosts can be supplied either as a comma-separated `Hosts` value or as
 * indexed values such as `Hosts__0`, `Hosts__1`, and so on.
 */
function loadOutboundHostValidatorOptionsFromEnv() {
    const result = {};
    const indexedHosts = new Map();
    const hosts = [];
    for (const [envKey, rawValue] of Object.entries(process.env)) {
        const upperKey = envKey.toUpperCase();
        if (!upperKey.startsWith(OUTBOUND_HOST_VALIDATOR_ENV_PREFIX_UPPER))
            continue;
        const property = envKey.substring(OUTBOUND_HOST_VALIDATOR_ENV_PREFIX.length);
        const upperProperty = property.toUpperCase();
        if (upperProperty === 'ENABLED') {
            const value = (0, env_1.parseBooleanEnv)(rawValue);
            if (value !== undefined)
                result.enabled = value;
        }
        else if (upperProperty === 'INCLUDEDEFAULTMICROSOFTHOSTS') {
            const value = (0, env_1.parseBooleanEnv)(rawValue);
            if (value !== undefined)
                result.includeDefaultMicrosoftHosts = value;
        }
        else if (upperProperty === 'HOSTS' && rawValue) {
            hosts.push(...rawValue.split(',').map(host => host.trim()).filter(Boolean));
        }
        else {
            const match = /^HOSTS__(\d+)$/i.exec(property);
            if (match && (rawValue === null || rawValue === void 0 ? void 0 : rawValue.trim()))
                indexedHosts.set(Number(match[1]), rawValue.trim());
        }
    }
    for (const [, host] of [...indexedHosts.entries()].sort(([left], [right]) => left - right)) {
        hosts.push(host);
    }
    if (hosts.length > 0)
        result.hosts = hosts;
    return result;
}
/** Creates the default validator, with explicit settings overriding the environment. */
function createOutboundHostValidator(options) {
    var _a, _b, _c;
    const fromEnv = loadOutboundHostValidatorOptionsFromEnv();
    return new OutboundHostValidator({
        enabled: (_a = options === null || options === void 0 ? void 0 : options.enabled) !== null && _a !== void 0 ? _a : fromEnv.enabled,
        includeDefaultMicrosoftHosts: (_b = options === null || options === void 0 ? void 0 : options.includeDefaultMicrosoftHosts) !== null && _b !== void 0 ? _b : fromEnv.includeDefaultMicrosoftHosts,
        hosts: (_c = options === null || options === void 0 ? void 0 : options.hosts) !== null && _c !== void 0 ? _c : fromEnv.hosts
    });
}
function getUrlHost(input) {
    if (!input)
        return undefined;
    try {
        const url = input instanceof URL ? input : new URL(input);
        return normalizeHostname(url.hostname);
    }
    catch {
        return undefined;
    }
}
function normalizeConfiguredHost(input) {
    let candidate = input === null || input === void 0 ? void 0 : input.trim();
    if (!candidate)
        return undefined;
    if (candidate.startsWith('*.'))
        candidate = candidate.slice(2);
    try {
        const absolute = new URL(candidate);
        if (absolute.hostname)
            return normalizeHostname(absolute.hostname);
    }
    catch {
        // A bare host, host:port, or host/path is handled below.
    }
    try {
        return normalizeHostname(new URL(`http://${candidate}`).hostname);
    }
    catch {
        return undefined;
    }
}
function normalizeHostname(host) {
    const normalized = host.trim().toLowerCase().replace(/\.$/, '');
    return normalized || undefined;
}
//# sourceMappingURL=outboundHostValidator.js.map