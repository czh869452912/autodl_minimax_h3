"use strict";
/**
 * Copyright (c) Microsoft Corporation. All rights reserved.
 * Licensed under the MIT License.
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.isPromise = isPromise;
exports.attempt = attempt;
/**
 * Detects promise-like values returned by internal loaders and callbacks.
 */
function isPromise(value) {
    return ((typeof value === 'object' || typeof value === 'function') &&
        value !== null &&
        'then' in value &&
        typeof value.then === 'function');
}
function attempt(options) {
    var _a;
    // Note: order of overloads ensures correct typing.
    let isAsync = false;
    try {
        const result = options.try();
        if (isPromise(result)) {
            isAsync = true;
            return result
                .catch(error => {
                var _a;
                if (!options.catch) {
                    throw error;
                }
                const result = (_a = options.catch) === null || _a === void 0 ? void 0 : _a.call(options, error);
                if (isPromise(result)) {
                    return result.then(() => undefined);
                }
                return undefined;
            })
                .finally(options.finally);
        }
        return result;
    }
    catch (error) {
        if (!options.catch) {
            throw error;
        }
        options.catch(error);
        return undefined;
    }
    finally {
        !isAsync && ((_a = options.finally) === null || _a === void 0 ? void 0 : _a.call(options));
    }
}
//# sourceMappingURL=attempt.js.map