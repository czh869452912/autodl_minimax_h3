"use strict";
/**
 * Copyright (c) Microsoft Corporation. All rights reserved.
 * Licensed under the MIT License.
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.traceFactory = traceFactory;
const agents_activity_1 = require("@microsoft/agents-activity");
const attempt_js_1 = require("../utils/attempt.js");
const category_js_1 = require("./category.js");
const constants_js_1 = require("./constants.js");
const noop_js_1 = require("../utils/noop.js");
const record_js_1 = require("../utils/record.js");
const errorHelper_js_1 = require("../errorHelper.js");
/**
 * Creates the trace helper bound to the package tracer.
 *
 * @remarks
 * - Only span names declared in `SpanNames` are accepted.
 * - Disabled span categories return a no-op context instead of creating a real span.
 */
function traceFactory(otel) {
    const validSpanNames = new Set(Object.values(constants_js_1.SpanNames));
    const tracer = otel.trace.getTracer('@microsoft/agents-telemetry');
    const trace = function trace(target, callback) {
        return runTrace(target, callback);
    };
    function runTrace(target, callback, parentSpan) {
        if (!target) {
            throw agents_activity_1.ExceptionHelper.generateException(Error, errorHelper_js_1.Errors.TraceDefinitionRequired);
        }
        if (!validSpanNames.has(target.name)) {
            throw agents_activity_1.ExceptionHelper.generateException(Error, errorHelper_js_1.Errors.UnrecognizedSpanName, undefined, { spanName: target.name });
        }
        if ((0, category_js_1.isSpanDisabled)(target.name)) {
            return callback ? (0, noop_js_1.noopContext)(callback) : (0, noop_js_1.noopContext)();
        }
        const execute = () => {
            const record = createRecord(target);
            if (!callback) {
                const span = tracer.startSpan(target.name);
                const flow = start(otel, target, span, record);
                const child = ((definition, childCallback) => runTrace(definition, childCallback, span));
                return {
                    record: flow.record,
                    actions: flow.actions,
                    child,
                    fail: flow.fail,
                    end: flow.end
                };
            }
            return tracer.startActiveSpan(target.name, span => {
                const flow = start(otel, target, span, record);
                return (0, attempt_js_1.attempt)({
                    try: () => callback({ record: flow.record, actions: flow.actions }),
                    catch: (error) => { throw flow.fail(error); },
                    finally: flow.end
                });
            });
        };
        if (!parentSpan) {
            return execute();
        }
        const parentContext = otel.trace.setSpan(otel.context.active(), parentSpan);
        return otel.context.with(parentContext, execute);
    }
    trace.define = definition => definition;
    return trace;
}
/**
 * Creates the mutable record stored for a trace execution.
 *
 * @remarks
 * - Plain objects are recursively merged.
 * - Arrays are copied and replaced.
 * - `undefined` values are ignored so definition defaults remain available.
 */
function createRecord(target) {
    const state = target.record ? (0, record_js_1.cloneRecordValue)(target.record) : {};
    return {
        set(values) {
            (0, record_js_1.mergeRecordValues)(state, values);
        },
        get() {
            return state;
        }
    };
}
/**
 * Initializes a trace execution and returns the action helpers plus end/fail controls.
 */
function start(otel, target, span, record) {
    var _a;
    var _b;
    const start = performance.now();
    let ended = false;
    let _error;
    return {
        record: record.set,
        actions: (_b = (_a = target.actions) === null || _a === void 0 ? void 0 : _a.call(target, { span })) !== null && _b !== void 0 ? _b : {},
        fail(error) {
            _error = error;
            return error;
        },
        end() {
            if (ended || !otel) {
                return;
            }
            ended = true;
            if (_error === undefined) {
                span.setStatus({ code: otel.SpanStatusCode.OK });
            }
            else {
                captureError(otel, span, _error);
            }
            try {
                target.end({
                    span,
                    record: record.get(),
                    duration: performance.now() - start,
                    error: _error,
                });
            }
            catch (endError) {
                captureError(otel, span, endError);
            }
            finally {
                span.end();
            }
        }
    };
}
/**
 * Records an error on the span and sets the span status to `ERROR`.
 *
 * @remarks
 * - Non-`Error` values are stringified so thrown primitives are still captured.
 */
function captureError(otel, span, error) {
    let message;
    if (error instanceof Error) {
        message = error.message;
        span.recordException(error);
    }
    else {
        message = String(error);
        span.recordException({ name: String(typeof error), message });
    }
    span.setStatus({ code: otel.SpanStatusCode.ERROR, message });
}
//# sourceMappingURL=trace.js.map