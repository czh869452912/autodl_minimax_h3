import type { NativeNodeKind } from "@copilotkit/channels-ui";
export interface SlackCatalogEntry {
    readonly component: string;
    readonly type: string;
    readonly kind: NativeNodeKind;
    readonly childrenSlot?: string;
    readonly required?: readonly string[];
    readonly source: string;
}
/**
 * Reviewed message-valid Slack Block Kit catalog — the blocks an app can
 * actually post. Two documented blocks are deliberately absent:
 *
 * - `alert`: "Alert blocks are currently only supported in modals." Verified by
 *   posting Slack's own example into a message and having it refused, while a
 *   plain section in the same delivery arrived.
 * - `file`: "You can't add this block to app surfaces directly, but it will
 *   show up when retrieving messages that contain remote files." The same
 *   sentence appears in `@slack/types`' own doc comment. It is an inbound
 *   shape, not an authorable one — offering it means offering a component that
 *   can never succeed. Sending a file is `thread.postFile()`.
 */
export declare const SLACK_BLOCK_MANIFEST: readonly [readonly ["Actions", "actions", "elements"], readonly ["Card", "card"], readonly ["Carousel", "carousel", "elements"], readonly ["Container", "container", "child_blocks"], readonly ["Context", "context", "elements"], readonly ["ContextActions", "context_actions", "elements"], readonly ["DataTable", "data_table", "rows"], readonly ["DataVisualization", "data_visualization"], readonly ["Divider", "divider"], readonly ["Header", "header", "text"], readonly ["Image", "image"], readonly ["Input", "input", "element"], readonly ["Markdown", "markdown"], readonly ["Plan", "plan", "tasks"], readonly ["RichText", "rich_text", "elements"], readonly ["Section", "section", "fields"], readonly ["Table", "table", "rows"], readonly ["TaskCard", "task_card", "tasks"], readonly ["Video", "video"]];
export declare const SLACK_ELEMENT_MANIFEST: readonly [readonly ["Button", "button"], readonly ["Checkboxes", "checkboxes", "options"], readonly ["ChannelsSelect", "channels_select"], readonly ["ConversationsSelect", "conversations_select"], readonly ["DatePicker", "datepicker"], readonly ["DateTimePicker", "datetimepicker"], readonly ["EmailInput", "email_text_input"], readonly ["ExternalSelect", "external_select"], readonly ["FeedbackButtons", "feedback_buttons"], readonly ["IconButton", "icon_button"], readonly ["Image", "image"], readonly ["MultiChannelsSelect", "multi_channels_select"], readonly ["MultiConversationsSelect", "multi_conversations_select"], readonly ["MultiExternalSelect", "multi_external_select"], readonly ["MultiStaticSelect", "multi_static_select", "option_groups"], readonly ["MultiUsersSelect", "multi_users_select"], readonly ["NumberInput", "number_input"], readonly ["Overflow", "overflow", "options"], readonly ["PlainTextInput", "plain_text_input"], readonly ["RadioButtons", "radio_buttons", "options"], readonly ["RichTextInput", "rich_text_input"], readonly ["StaticSelect", "static_select", "option_groups"], readonly ["TimePicker", "timepicker"], readonly ["UrlInput", "url_text_input"], readonly ["UsersSelect", "users_select"], readonly ["WorkflowButton", "workflow_button"]];
export declare const SLACK_OBJECT_MANIFEST: readonly [readonly ["ConfirmationDialog", "confirm"], readonly ["ConversationFilter", "conversation_filter"], readonly ["DispatchActionConfig", "dispatch_action_config"], readonly ["MarkdownText", "mrkdwn"], readonly ["Option", "option"], readonly ["OptionGroup", "option_group", "options"], readonly ["PlainText", "plain_text"], readonly ["RichTextList", "rich_text_list", "elements"], readonly ["RichTextPreformatted", "rich_text_preformatted", "elements"], readonly ["RichTextQuote", "rich_text_quote", "elements"], readonly ["RichTextSection", "rich_text_section", "elements"], readonly ["RichTextText", "text"], readonly ["SlackFile", "slack_file"], readonly ["Trigger", "trigger"], readonly ["Workflow", "workflow"]];
/**
 * Composition objects that carry **no** `type` discriminator in Slack's schema.
 * Text and rich-text nodes are tagged (`plain_text`, `mrkdwn`, `text`,
 * `rich_text_section`, …); the rest are plain structures — an option is
 * `{text, value}`, a confirm dialog is `{title, text, confirm, deny}`. Emitting
 * a `type` on those is an unknown field and Slack refuses the entire message,
 * which took out every select, checkbox, radio group and overflow menu authored
 * through this catalog.
 *
 * The manifest still needs the type string as its lookup key, so the exclusion
 * is recorded here rather than by leaving the entry untyped.
 */
export declare const SLACK_UNTYPED_OBJECTS: ReadonlySet<string>;
export declare const SLACK_NATIVE_MANIFEST: readonly SlackCatalogEntry[];
//# sourceMappingURL=native-manifest.d.ts.map