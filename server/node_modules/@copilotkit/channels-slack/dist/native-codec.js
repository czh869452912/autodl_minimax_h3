import { isNativeNode } from "@copilotkit/channels-ui";
import { validateSlackDataVisualization } from "./data-visualization.js";
import { SLACK_NATIVE_MANIFEST, SLACK_UNTYPED_OBJECTS, } from "./native-manifest.js";
const manifest = new Map(SLACK_NATIVE_MANIFEST.map((entry) => [`${entry.kind}:${entry.type}`, entry]));
const HANDLERS = new Set(["onClick", "onSelect", "onSubmit"]);
const REQUIRED_FIELDS = {
    actions: ["elements"],
    button: ["text"],
    context: ["elements"],
    data_visualization: ["title", "chart"],
    header: ["text"],
    image: ["alt_text"], // plus image_url OR slack_file — checked below
    input: ["label", "element"],
    option: ["text", "value"],
    video: ["alt_text", "thumbnail_url", "title", "title_url", "video_url"],
};
/** Serialize and validate one traversable Slack native node. */
export function serializeSlackNativeNode(node, path = "Slack") {
    if (node.props.provider !== "slack") {
        throw new Error(`${path}: Slack delivery cannot render Teams native JSX.`);
    }
    if (node.props.nativeKind === "raw") {
        assertRawIsNonInteractive(node.props.value, `${path}.Raw`);
        if (!isRecord(node.props.value)) {
            throw new Error(`${path}.Raw: value must be a Slack object.`);
        }
        return node.props.value;
    }
    const entry = manifest.get(`${node.props.nativeKind}:${node.props.nativeType}`);
    if (!entry) {
        throw new Error(`${path}: unknown Slack ${node.props.nativeKind} ${node.props.nativeType}.`);
    }
    // Untyped composition objects are plain structures; tagging them with a
    // `type` makes Slack reject the message that contains them.
    const output = entry.kind === "object" && SLACK_UNTYPED_OBJECTS.has(entry.type)
        ? {}
        : { type: entry.type };
    for (const [name, value] of Object.entries(node.props)) {
        if (name === "provider" ||
            name === "nativeKind" ||
            name === "nativeType" ||
            HANDLERS.has(name)) {
            continue;
        }
        if (name === "children") {
            if (value !== undefined && !entry.childrenSlot) {
                throw new Error(`${path}.${entry.component}: children are not allowed.`);
            }
            if (entry.childrenSlot) {
                output[entry.childrenSlot] = serializeValue(value, `${path}.${entry.component}.${entry.childrenSlot}`);
            }
            continue;
        }
        output[name] = serializeValue(value, `${path}.${entry.component}.${name}`);
    }
    const handler = handlerOf(node.props);
    if (handler)
        output.action_id = handler.id;
    if (output.value !== undefined && typeof output.value !== "string") {
        output.value = JSON.stringify(output.value);
    }
    validateRequiredFields(entry.component, entry.type, output, path);
    if (entry.type === "data_visualization") {
        validateSlackDataVisualization(output, path);
    }
    return output;
}
function validateRequiredFields(component, type, output, path) {
    for (const field of REQUIRED_FIELDS[type] ?? []) {
        if (output[field] === undefined) {
            throw new Error(`${path}.${component}.${field} is required.`);
        }
    }
    if (type === "section" &&
        output.text === undefined &&
        output.fields === undefined) {
        throw new Error(`${path}.${component} requires text or fields.`);
    }
    // An image points at either an external URL or a file already in the
    // workspace. Demanding `image_url` unconditionally made the `slack_file` form
    // impossible to build.
    if (type === "image" &&
        output.image_url === undefined &&
        output.slack_file === undefined) {
        throw new Error(`${path}.${component} requires image_url or slack_file.`);
    }
}
/** Read useful plain text from native nodes for Slack notifications and a11y. */
export function slackNativeText(node) {
    for (const field of [
        node.props.text,
        node.props.title,
        node.props.alt_text,
    ]) {
        const direct = textValue(field);
        if (direct)
            return direct;
    }
    const pieces = [];
    visit(node.props, (name, value) => {
        if ((name === "text" || name === "title" || name === "alt_text") &&
            typeof value === "string") {
            pieces.push(value.replaceAll("*", ""));
        }
    });
    return pieces.join(" ").replace(/\s+/g, " ").trim();
}
/** Derive the same short Slack fallback for direct and managed delivery. */
export function slackFallbackText(ir) {
    const rootFallback = ir.find((node) => node.type === "message" && typeof node.props.fallbackText === "string")?.props.fallbackText;
    if (typeof rootFallback === "string" && rootFallback.trim()) {
        return truncateFallback(rootFallback);
    }
    const header = findPortableNode(ir, "header");
    const source = header ? portableText(header) : firstTreeText(ir);
    return truncateFallback(source || "…");
}
function truncateFallback(value) {
    const text = value.replace(/\s+/g, " ").trim();
    if (!text)
        return "…";
    return text.length > 150 ? `${text.slice(0, 149)}…` : text;
}
function firstTreeText(nodes) {
    for (const node of nodes) {
        const text = isNativeNode(node)
            ? slackNativeText(node)
            : portableText(node);
        if (text.trim())
            return text;
    }
    return "";
}
function portableText(node) {
    if (node.type === "text")
        return String(node.props.value ?? "");
    const children = channelChildren(node.props.children);
    return children.map(portableText).filter(Boolean).join(" ");
}
function findPortableNode(nodes, type) {
    for (const node of nodes) {
        if (node.type === type)
            return node;
        const found = findPortableNode(channelChildren(node.props.children), type);
        if (found)
            return found;
    }
    return undefined;
}
function channelChildren(value) {
    if (Array.isArray(value))
        return value.filter(isChannelNode);
    return isChannelNode(value) ? [value] : [];
}
function isChannelNode(value) {
    return (typeof value === "object" &&
        value !== null &&
        "type" in value &&
        "props" in value);
}
function textValue(value) {
    if (typeof value === "string")
        return value.replaceAll("*", "").trim();
    if (isNativeNode(value))
        return slackNativeText(value);
    return "";
}
function serializeValue(value, path) {
    if (isNativeNode(value))
        return serializeSlackNativeNode(value, path);
    if (Array.isArray(value)) {
        return value.map((item, index) => serializeValue(item, `${path}[${index}]`));
    }
    return value;
}
function handlerOf(props) {
    for (const name of HANDLERS) {
        const handler = props[name];
        if (isRecord(handler) &&
            typeof handler.id === "string" &&
            handler.id.length > 0) {
            return { id: handler.id };
        }
    }
    return undefined;
}
function assertRawIsNonInteractive(value, path) {
    visit(value, (name, current) => {
        if (HANDLERS.has(name) || typeof current === "function") {
            throw new Error(`${path}: raw payloads cannot contain SDK callbacks.`);
        }
    });
}
function visit(value, visitor) {
    if (Array.isArray(value)) {
        for (const child of value)
            visit(child, visitor);
        return;
    }
    if (!isRecord(value))
        return;
    for (const [name, child] of Object.entries(value)) {
        visitor(name, child);
        visit(child, visitor);
    }
}
function isRecord(value) {
    return typeof value === "object" && value !== null && !Array.isArray(value);
}
