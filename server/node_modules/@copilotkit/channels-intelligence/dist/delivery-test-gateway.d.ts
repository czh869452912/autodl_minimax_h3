import type { ChannelDeliveryPacket } from "./delivery-contracts.js";
import type { PreparedChannelDelivery } from "./delivery-transport.js";
import type { RealtimeGatewayDeliveryChannel, RealtimeGatewaySession } from "./realtime-gateway.js";
/** In-memory control and delivery topics for SDK integration tests. */
export declare class DeliveryTestGateway implements RealtimeGatewaySession {
    readonly controlPushes: Array<{
        event: string;
        payload: unknown;
    }>;
    readonly packets: ChannelDeliveryPacket[];
    private invitationHandler?;
    private currentDelivery?;
    private leaves;
    push(event: string, payload: unknown): Promise<unknown>;
    on(event: string, handler: (payload: unknown) => void): void;
    join(_topic: string, _payload: unknown): Promise<RealtimeGatewayDeliveryChannel>;
    /** Publish one prepared delivery and wait for its delivery topic to close. */
    deliver(delivery: PreparedChannelDelivery): Promise<void>;
}
/** Build one valid prepared delivery for SDK integration tests. */
export declare function preparedDelivery(suffix: string, adapter: "slack" | "teams", input: PreparedChannelDelivery["turn"]["input"] | {
    kind: "text";
    text?: string;
    messageRef?: {
        id: string;
    };
    files?: Extract<PreparedChannelDelivery["turn"]["input"], {
        kind: "text";
    }>["files"];
}): PreparedChannelDelivery;
//# sourceMappingURL=delivery-test-gateway.d.ts.map