export interface ChannelDeliveryChargeClientOptions {
    readonly baseUrl: string;
    readonly apiKey: string;
    readonly fetch?: typeof globalThis.fetch;
}
/** Idempotent App API client for one delivery's first substantive work. */
export declare class ChannelDeliveryChargeClient {
    private readonly options;
    private readonly baseUrl;
    private readonly fetchFn;
    constructor(options: ChannelDeliveryChargeClientOptions);
    charge(deliveryId: string): Promise<void>;
}
//# sourceMappingURL=delivery-charge.d.ts.map