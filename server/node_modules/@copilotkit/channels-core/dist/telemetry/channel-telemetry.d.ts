import type { LambdaSendOptions } from "@copilotkit/shared";
import type { StateStore } from "../state/state-store.js";
export declare const CHANNEL_TELEMETRY_EVENTS: readonly ["oss.channel.configured", "oss.channel.started", "oss.channel.start_failed", "oss.channel.agent_run", "oss.channel.agent_run_failed"];
export type ChannelTelemetryEvent = (typeof CHANNEL_TELEMETRY_EVENTS)[number];
export declare function isTestEnv(): boolean;
export declare function resolveEnvironment(): string;
export interface ChannelTelemetryOptions {
    backend: StateStore;
    packageName: string;
    packageVersion: string;
    environment?: string;
    disabled?: boolean;
    sessionId?: string;
    send?: (o: LambdaSendOptions) => Promise<void>;
    resolveId?: () => Promise<string>;
}
export declare class ChannelTelemetry {
    private readonly opts;
    private readonly disabled;
    private readonly sendFn;
    private readonly sessionId;
    private readonly environment;
    private readonly resolveId;
    private idPromise?;
    private static disclosed;
    constructor(opts: ChannelTelemetryOptions);
    capture(event: ChannelTelemetryEvent, properties: Record<string, unknown>): void;
    private disclose;
    private dispatch;
}
//# sourceMappingURL=channel-telemetry.d.ts.map