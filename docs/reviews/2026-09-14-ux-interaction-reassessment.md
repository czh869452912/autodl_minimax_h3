# UX 交互审查调整与验收记录（2026-09-14）

对应 [原审查](2026-09-14-ux-interaction-review.md)。在首轮复核后，根据“完成全部的调整”继续实施；下表覆盖原报告全部 114 个页面编号，S1–S7 和 Top 5 不重复计数。本文件替代此前“仅修复第一批”的评估状态。

## 处理原则与能力边界

- 保留正确的数据保护行为：超长文本软校验、正常重复生成、独立任务计时和已有历史未读标记；不把这些行为当作已证实的缺陷。
- AutoDL 官方 [ComfyUI API](https://autodl.art/docs/comfyui_api/) 公开 submit/status，未公开远端取消。实现本地未提交取消及下载/待保存取消；不声称停止服务端执行或免除计费。
- 版本移除使用持久隐藏标记，保留底层版本及已导出的引用；完整数据库备份不包含视频字节或安全存储密钥，界面已说明。
- 本次明确使用浅色；没有宣称实现完整深色主题。性能只做计时组件隔离，不将推测写成实测性能提升。

## 逐项处置

“完成”表示代码行为已调整，验证范围以最后一节为准；不等同于所有机型、网络服务和无障碍服务均已验收。

### 生成：27 项

| 原编号 | 处置 | 最终行为 |
|---|---|---|
| H1 | 完成 | 字段、素材删除与提交状态统一锁定。 |
| H2 | 完成 | 目录加载、空目录、失败分态；重试及前往设置入口。 |
| H3 | 调整方案 | 保留参数支持变体生成；提交门闩防重入，成功可留在当前页。 |
| H4 | 完成 | 字段错误滚动定位、文本框聚焦；非字段错误保留总结。 |
| M1 | 完成 | 切换进度和锁定说明。 |
| M2 | 完成 | 工作流选择使用可关闭、可拖动抽屉。 |
| M3 | 完成 | 逐文件筛选，合格文件保留；汇总格式、数量、大小和总预算拒绝原因。 |
| M4 | 完成 | 图片角标统一 @imageN。 |
| M5 | 完成 | 丢弃确认；素材删除可撤销，持久表单保留撤销素材及 CAS 引用。 |
| M6 | 完成 | 键盘避让、拖动收键盘、焦点定位。 |
| M7 | 完成 | 外部 Prompt 改为替换/追加/取消；动作基于最新编辑值。 |
| M8 | 完成 | 错误字号、alert/liveRegion、输入错误提示及枚举语义。 |
| M9 | 完成 | 素材改变后重验相关约束，不无条件抹除错误。 |
| M10 | 完成 | 明确浅色策略；Android 原生主题同步为 Light。 |
| M11 | 完成 | 加载、切换、提交和音频准备指示。 |
| M12 | 完成 | 交接操作使用描边按钮、可读字号。 |
| L1 | 保留正确行为 | 保留超长文本和软校验，避免原生 maxLength 静默丢输入。 |
| L2 | 完成 | 空整数输入以 schema minimum 作为步进基值。 |
| L3 | 完成 | 随机种子说明明确留空后每次随机。 |
| L4、L5 | 完成 | 版本参数使用选中态、radio 和 schema.title。 |
| L6 | 完成 | 删除 CreateForm 无引用样式。 |
| L7、L8 | 完成 | 音频加载/播放进度；图片缩放预览、独立删除区。 |
| L9 | 完成 | 达到图片/音频上限时提示先删除再添加。 |
| L10 | 完成 | 默认渲染器注册表复用。 |
| L11 | 完成 | 交接提示改为可读摘要，不显示内部 ID。 |

### Prompt 助手：24 项

| 原编号 | 处置 | 最终行为 |
|---|---|---|
| H1、H2 | 完成 | 长草稿不截断；提交中与运行中分态，停止有守卫。 |
| H3 | 调整方案 | 保留现有消息投影缓存；运行计时下沉到独立组件，过程文本 memo。未将静态分析称为性能测量。 |
| M1 | 完成 | 普通提示去重、可关闭；持久化错误不能关闭，写入成功后清除。 |
| M2 | 完成 | 距底部判断跟随，浏览历史时显示新内容提示，点击返回最新。 |
| M3 | 完成 | 区分提交失败与运行失败；前者重发当前草稿，后者按 runId 重试。 |
| M4 | 已有能力复核 | sessionRunLabel 已消费 h3ReadAt，完成时间较新时显示未读；保留该链路。 |
| M5、M6 | 完成 | 会话/回答/参考图读屏使用标题或摘要；管理触控区至少 48。 |
| M7 | 完成 | 上传 spinner、等待原因、删除附件取消入口。 |
| M8 | 完成 | 图片预览支持双指缩放、双击放大、平移、下滑关闭及可访问按钮。 |
| M9 | 完成 | 回车换行、全屏编辑、完整文本保留。 |
| M10 | 完成 | 菜单统一管理；空名称内联错误，异步重命名成功后关闭，重复提交门闩。 |
| M11 | 完成 | 刷新失败保留已安装工作流；运行会话删除提示停止；兜底页面滚动与标题。 |
| L1、L2、L3 | 完成 | 返回最新提示、历史日期、搜索清除与无匹配提示。 |
| L4 | 保留可用方案 | 原生来源选择 Alert 已支持系统返回/取消，无需为两个来源重复造菜单。 |
| L5 | 完成 | 处理过程添加 button/expanded；装饰箭头不参与朗读。 |
| L6 | 完成 | 状态放入 footer；加载更早版本后横向滚动到入口。 |
| L7 | 完成 | 版本时间戳、确认移除列表；保留原记录以保护已有 handoff/run 引用，隐藏状态持久化。 |
| L8 | 完成 | 运行错误统一告警色和 alert。 |
| L9 | 完成 | 灵感卡数据驱动分行，使用主题色。 |
| L10 | 完成 | 入口改为查看 Prompt 版本，预览后带入生成。 |

### 任务：18 项

| 原编号 | 处置 | 最终行为 |
|---|---|---|
| H1 | 完成（接口边界内） | 仅取消未 claim 且 attempt=0 的本地提交；事务内检查并终止操作。重复取消幂等。已开始提交明确不可取消。 |
| H2 | 完成 | 下载数值、0%、进度条及取消；取消撤销 lease、阻止重试并停止原生传输。待保存可取消，已开始发布不可中断。 |
| H3 | 完成 | 手动刷新圈与自动维护分离。 |
| H4 | 完成 | 详情中文状态、错误说明、下载及重试入口。 |
| M1 | 完成 | 维护命令错误独立内联反馈，避免重复 Alert。 |
| M2 | 完成 | 分页等待、错误重试、到底状态。 |
| M3 | 完成 | 冷启动指示、错误重试、空态生成 CTA。 |
| M4 | 完成 | 详情区域加大，并提供查看详情提示。 |
| M5 | 完成 | 监控激活样式、启动门闩和操作中状态。 |
| M6 | 完成 | 网络、权限、地址、存储错误中文映射；详情可复制诊断。 |
| M7 | 完成 | 成功/失败/执行状态色及中文标记，活动下载 spinner。 |
| M8 | 保留正确行为 | TaskTiming 原已独立 memo，不每秒重绘整卡；保留包含本地等待的真实时长。 |
| L1、L2 | 完成 | 列表短 ID、创建时间；详情保留诊断能力。 |
| L3 | 完成 | 正文次要色加深，关键 11px 标签升至 13px。 |
| L4 | 完成 | 全部/活动/失败筛选进入数据库查询，覆盖所有分页。 |
| L5 | 完成 | 移除触控区域和确认保留；滑动删除不是必要入口。 |
| L6 | 完成 | 冷启动有明确加载指示，未额外叠加无信息骨架。 |

### 结果与播放：24 项

| 原编号 | 处置 | 最终行为 |
|---|---|---|
| H1 | 完成 | 焦点与投影订阅刷新异步导出状态；失败可重试。 |
| H2 | 完成 | 300ms 防抖、查询代次、首屏/分页互斥；清除尚未发出的搜索仍能完成加载。 |
| H3 | 完成 | schema v10 删除意图、资产/引用/发布写入保护；阻止旧 upsert 和重建复活，保留同任务其他作品及共享文件。 |
| M4 | 完成 | 原生静音按钮随播放器进入全屏。 |
| M5 | 完成 | 下载中文数值含 0%。 |
| M6 | 完成 | 提供下载/重试；生产命令先获取新结果地址，再持久提交下载。 |
| M7 | 完成 | 区分损坏、解码和源不可访问；网络离线后恢复仅自动重试一次，仍保留手动恢复。 |
| M8 | 完成 | 准备中 SQL 同时包括 queued/downloading；列表不改 durable 失败状态。 |
| M9 | 完成 | 选择绑定查询/筛选、取消与选择已加载项。 |
| M10 | 完成 | 统一网格比例并 contain，竖视频封面不裁切。 |
| M11 | 完成 | 原生 ACTION_SEND + FileProvider content URI、MIME、ClipData 和临时读权限。 |
| M12、M14 | 完成 | 中文状态和焦点刷新。 |
| M13 | 完成 | 首屏/分页独立错误及重试，读取失败不冒充空结果。 |
| L15、L16 | 完成 | 无源提示准确；空库、无匹配、加载与失败区分。 |
| L17 | 完成 | 复制成功非阻断反馈；写入成功不再依赖回读。 |
| L18 | 完成 | 错误 liveRegion；原生播放/进度/全屏等控制器中文读屏文案。 |
| L19 | 完成 | 原生加载指示属于 playerView，随全屏迁移；错误退出全屏以显示恢复操作。 |
| L20、L21 | 完成 | 导航防重入、焦点回来解除锁，页面标题统一结果。 |
| L22 | 完成 | 只对本地视频串行提帧，缓存复用；只更新匹配路径的 poster 字段。 |
| L23 | 完成/保留 | 保留生命周期保护，增加正常播放时外部播放器入口；进度拖动由播放器处理。 |
| L24 | 完成 | 筛选 radio 语义，作品可通过 accessibilityActions 进入多选。 |

### 设置与全局：21 项

| 原编号 | 处置 | 最终行为 |
|---|---|---|
| H1 | 完成 | 加密草稿独立于生效设置，重开可恢复/丢弃，串行写防旧值覆盖；保存失败保留输入。 |
| H2 | 完成 | 固定保存栏，键盘避让与焦点滚动。 |
| H3 | 完成 | 旧库二次确认、先备份再清理选项；正常设置提供完整数据库备份入口。 |
| M4 | 完成 | 校验返回字段集合；错误高亮/行内说明与聚焦，跨字段预算指向两项。 |
| M5 | 完成 | 密钥默认隐藏，可显隐，关闭自动纠错及大写。 |
| M6 | 完成 | 同步失败/部分成功告警及重复同步入口。 |
| M7 | 完成 | 完整备份列表选择、确认、恢复前 integrity_check。 |
| M8 | 完成 | 恢复操作锁定、spinner 与重开说明。 |
| M9 | 完成 | 备份/恢复/清理入口；维护请求持久化，停止监控，重启时执行并重新迁移，执行器检测维护请求。 |
| M10 | 完成 | 外链、高级设置与同步按钮至少 44/48。 |
| M11 | 完成 | 输入控件边框与装饰分隔线分开，次要文字加深。 |
| M12 | 完成 | 把手与标题拖动、顶部内容联动、快速下滑关闭；保留关闭按钮。 |
| L13 | 完成 | 恢复页改浅色与应用一致。 |
| L14、L15 | 保留一致方案 | 使用原生 Switch 与现有圆角卡片体系，保留平台开关可访问性。 |
| L16 | 完成 | 删除未使用 AppHeader。 |
| L17、L19 | 完成 | 保存状态非阻断、拖动收键盘。 |
| L18 | 完成 | 保留背景/字重/selected，Tab 标签提升至 13px。 |
| L20 | 完成 | 旧库等待期间显示启动说明，仍阻止业务页面/执行器。 |
| L21 | 完成 | 遮罩关闭节点不进入读屏树，保留明确关闭按钮及 accessibilityEscape。 |

## 数据一致性与迁移

数据库升级至 v10。删除意图与资产删除在同一事务内完成；数据库触发器拦截迟到资产、工作流引用及发布记录。修复流程仍保留其他 artifact；CAS 只释放所属引用，非 CAS 文件只有不存在其他资产持有时才删除。删除意图随数据库备份/恢复保留，清库时清除。

冻结 v5–v8 的迁移结果与新库保持一致，原 v9 schema fixture 未改写；另覆盖 v9 到 v10 的迁移及触发器安装。恢复备份后重新打开数据库并运行迁移，避免直接放行业务写入旧结构。

## 验证记录

- TypeScript：通过。
- 全量 Jest：162 suites / 1164 tests 通过；仓库原有 1 suite / 2 tests 跳过。
- Android x86_64 debug APK：构建通过，已安装到 Resizable_Experimental 模拟器。
- 新增回归：加密草稿写入顺序和格式；删除后两轮 reconciliation、迟到 upsert/引用/发布、同任务其他作品、fallback 不复活；取消前置条件/幂等/lease 撤销；搜索过期响应及防抖期间清除；预算错误字段关联。
- 最终针对性回归：6 suites / 102 tests 通过；最终 TypeScript 与 Android 构建通过。
- Android 模拟器：现有数据库升级后启动成功；工作流把手/标题上滑展开、顶部内容联动、快速下滑关闭、关闭按钮均通过 UI tree 验证。固定保存栏及输入聚焦后保存入口可达。
- 本地视频详情可播放；静音切换为“开启声音”后进入全屏仍保持；原生播放/全屏读屏文案已中文化；ACTION_SEND 打开系统“Sharing 1 file”面板后取消。未向外发送视频。
- 设备截图：[抽屉展开](ux-2026-09-14/workflow-sheet.png)、[全屏静音](ux-2026-09-14/fullscreen.png)、[设置保存栏](ux-2026-09-14/settings-save.png)。
- 没有调用真实生成或付费 LLM 请求，没有执行用户数据清库/恢复。
- 尚未进行 iOS 真机、TalkBack 实际朗读、低端机长对话帧时和网络服务端取消验收。这些属于验证范围限制，不伪装成已通过。


## 二次核验反馈闭环（2026-09-14）

用户提供的五区域独立核验结论为：修复真实、无虚假声明，初轮 TypeScript 和 162 suites / 1164 tests 与验收记录一致。以下是针对新发现问题的追加修复，不覆盖上方初轮设备验收的时间范围。

| 新发现 | 追加处置 | 回归证据 |
|---|---|---|
| 中-1：坏草稿锁死设置页 | 草稿读取失败与生效设置读取分离；坏 JSON、字段类型错误、旧版不识别的新键均隔离，仍可编辑/保存已有配置。提供单独丢弃按钮，丢弃写入失败时保留恢复入口；隔离期间不自动覆盖原草稿，也不宣称草稿已保存。 | settings route 的 3 个参数化回归涵盖坏 JSON、坏类型、新键回滚，以及丢弃失败后再次成功、后续正常草稿写入。 |
| 中-2：Agent 全局错误替换整屏 | 配置错误继续用于配置页；搜索、分页、重命名的 Promise 直接交给局部 UI。创建/删除错误使用可关闭提示，不更换会话组件、不释放运行时。首次恢复错误保留新建对话入口。 | AgentScreen 生产接线测试：三类失败均向局部调用方拒绝，组件实例、当前会话和列表引用不变，运行时未 dispose。 |
| 分页失败重试错位 | 首屏搜索与分页各自保存错误；分页错误按钮重试相同查询和已有 offset，不回到第一页。父组件不再吞掉分页 Promise；查询变化使旧分页 UI 回调失效。 | HistoryList 测试：页尾自动触发不循环重试，手动重试只调用分页、原行保留。 |
| 数据维护失败锁定、坏请求无自救 | 持久排程失败解锁操作；排程成功但停监控失败保留维护锁并提供重试退出，避免重新排程或继续写库。启动阶段只允许单独丢弃明确解析/结构损坏的请求，不把普通 IO 或执行失败误当成可丢弃请求。 | DataManagementPanel 排程/停监控分阶段失败回归；RootLayout 坏请求丢弃后进入应用，未调用清库或恢复。 |
| ImagePreview 手势跳变/无限平移/接近 1 无法关闭 | 使用实时变换引用；双指变单指时以剩余手指当前位置重新建立拖动原点；按视口和缩放比例硬性限制平移范围。小于 1.01 的缩放归一到 1；捏合手势本身不触发误关闭，后续单指下滑可关闭。 | 手势回调回归覆盖续拖不跳变、边界钳制、近 1 缩放与下滑关闭；使用硬边界而非回弹动画。 |
| 画廊选择被首屏刷新裁剪 | 相同查询刷新不再以第一页截断选择；变更查询/筛选仍显式清空选择；补回搜索 placeholderTextColor。 | 全量路由测试通过；本轮未新增选择刷新专用测试。 |
| Create 校验滚动非空断言 | 先确认 ScrollView 引用存在再测量，移除非空断言，未挂载时保留校验结果。 | TypeScript、现有 Create 回归通过。 |
| M11 选项边框、Light 配置 | 设置选项、工作流枚举/步进/开关选项使用控件边框 token；Agent 参数预设边框一致。expo.userInterfaceStyle 设为 light，与 Android 已有 Light 设置一致。 | TypeScript 和现有 UI/工作流回归通过。 |

### 本轮验证与覆盖边界

- 全量 Jest：**165 suites / 1176 tests 通过**；原有 **1 suite / 2 tests 跳过**。较初轮增加 3 个测试文件、12 个用例。
- TypeScript：通过。
- 新增任务取消 UI 回归：确认前不调用取消命令、确认后传递正确 ID、busy 时禁用、提交开始后不再显示取消入口。
- 新增播放自动恢复回归：首次 online 不触发；远程源不可用后观察到 offline → online 仅自动重试一次；重复网络抖动不重复重试；解码失败不自动重试；卸载移除监听。
- 原有取消竞态测试仍是顺序状态机覆盖，本次 UI 回归不等同于多连接真并发压力测试。
- MediaPicker 逐文件筛选、撤销素材持久化、Create 重试/聚焦/替换追加、Agent submit 分流、版本隐藏过滤等，仍有未专门补齐的交互自动化用例；维护已补失败分支，不宣称已覆盖完整真实进程重启链路。
- 本轮图片手势验证是组件级事件回归，未重复 Android 多指真机手势验收；上方 Android 截图和构建记录属于初轮验证。没有执行真实生成、付费 LLM、清库或恢复操作。


### 提交前复核

用户再次逐项核验上述追加修复，确认全部属实、无阻塞问题，并授权一次提交。当前代码与已通过的 TypeScript、165 suites / 1176 tests 对应，提交前 diff 空白检查通过。画廊焦点刷新时的短暂加载提示、退出后的备用提示，以及上列自动化和设备验证边界作为非阻塞事项保留。
